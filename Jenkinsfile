pipeline {
    agent any
    stages {
        stage('Custom Script') {
            steps {
                // Run a custom script directly on the host
                sh 'echo "This is running on the host machine."'
            }
        }
    }
}
